// eslint-disable-next-line react/no-unnecessary-use-prefix -- must match next/navigation API
export function usePathname() {
  return "/warrior/arms/pvp/2v2"
}

// eslint-disable-next-line react/no-unnecessary-use-prefix -- must match next/navigation API
export function useRouter() {
  return {
    push: () => {},
    replace: () => {},
    refresh: () => {},
    back: () => {},
    forward: () => {},
    prefetch: () => {},
  }
}
